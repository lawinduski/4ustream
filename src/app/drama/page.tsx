'use client';
import { useEffect,useState } from 'react'; import { PageShell } from '@/components/PageShell'; import { Protected } from '@/components/Protected'; import { MediaCard } from '@/components/MediaCard'; import { getMedia } from '@/lib/content'; import type { MediaItem } from '@/lib/types'; import { useApp } from '@/components/AppProvider';
export default function Drama(){const {t}=useApp();const [items,setItems]=useState<MediaItem[]>([]);useEffect(()=>{getMedia().then(x=>setItems(x.filter(i=>i.type==='drama'&&i.enabled!==false))).catch(()=>{});},[]);return <PageShell><Protected><div className="space-y-8"><div><h1 className="text-4xl font-black">{t('drama')}</h1><p className="text-slate-400 mt-2">Watch your active authorized drama catalog.</p></div><div className="grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-5 gap-4">{items.map(x=><MediaCard key={x.id} item={x}/>)}</div>{!items.length&&<div className="text-center py-20 text-slate-500">No drama is active yet.</div>}</div></Protected></PageShell>}
