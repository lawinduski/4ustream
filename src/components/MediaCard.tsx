import Link from 'next/link';
import { Play } from 'lucide-react';
import type { MediaItem } from '@/lib/types';
export function MediaCard({item}:{item:MediaItem}){return <Link href={`/watch?media=${encodeURIComponent(item.id)}`} className="block glass card-hover rounded-2xl overflow-hidden group"><div className="relative aspect-[2/3] bg-slate-900 overflow-hidden"><img src={item.poster} alt={item.title} className="w-full h-full object-cover" loading="lazy"/><div className="absolute inset-0 bg-black/0 group-hover:bg-black/45 transition grid place-items-center"><span className="h-12 w-12 rounded-full bg-white text-slate-900 grid place-items-center opacity-0 group-hover:opacity-100 transition shadow-xl"><Play size={18} fill="currentColor"/></span></div></div><div className="p-4"><div className="text-xs text-violet-300 font-semibold">{item.year} · {item.genre}</div><h3 className="font-bold mt-1 truncate">{item.title}</h3><p className="text-xs text-slate-400 mt-2 line-clamp-2">{item.description}</p></div></Link>}
