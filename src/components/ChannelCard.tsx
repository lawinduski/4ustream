'use client';
import Link from 'next/link';
import { Play, Star } from 'lucide-react';
import type { Channel } from '@/lib/types';
export function ChannelCard({channel}:{channel:Channel}){ const fav=()=>{const k='4u-favorites';const a=JSON.parse(localStorage.getItem(k)||'[]');const n=a.includes(channel.id)?a.filter((x:string)=>x!==channel.id):[...a,channel.id];localStorage.setItem(k,JSON.stringify(n));}; return <div className="glass card-hover rounded-2xl overflow-hidden group relative"><div className="aspect-[16/10] bg-gradient-to-br from-slate-900 to-slate-800 grid place-items-center">{channel.logo?<img src={channel.logo} alt="" className="absolute inset-0 w-full h-full object-cover opacity-80"/>:<div className="h-16 w-16 rounded-2xl bg-white/5 border border-white/10 grid place-items-center"><span className="font-black text-lg text-slate-300">{channel.name.slice(0,2).toUpperCase()}</span></div>}<button onClick={fav} className="absolute top-3 end-3 p-2 rounded-xl bg-black/30 text-slate-300 hover:text-yellow-300 z-10" aria-label="Favorite"><Star size={16}/></button><Link href={`/live?channel=${encodeURIComponent(channel.id)}`} className="absolute inset-0 grid place-items-center opacity-0 group-hover:opacity-100 transition bg-black/35"><span className="h-12 w-12 rounded-full bg-white text-slate-900 grid place-items-center shadow-xl"><Play size={19} fill="currentColor"/></span></Link></div><div className="p-4"><div className="text-[10px] uppercase tracking-[.18em] text-violet-300 font-bold">{channel.category}</div><h3 className="font-bold mt-1 truncate">{channel.name}</h3><p className="text-xs text-slate-400 mt-1">Ready to watch</p></div></div> }
